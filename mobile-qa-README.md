# 모바일 QA 게이트

배포 전에 프로젝트 루트에서 **한 줄**:

```bash
node mobile-qa-check.mjs
```

전 화면(자동 발견)을 412px·360px 실제 브라우저로 열어 아래를 좌표로 측정한다.
하나라도 걸리면 종료 코드 1 — 그 상태로는 배포하지 않는다.

| 검사 | 무엇을 재나 | 실제로 났던 사례 |
|---|---|---|
| 화면 밖 요소 | 요소 오른쪽 끝 > 화면 폭 (가로 스크롤 줄 제외) | 해외 거래·주문 전체 잘림(v17.6) · 분봉 버튼 잘림 |
| 글자 잘림 | 잎 노드 scrollWidth/Height > client (말줄임 의도 제외) | ETF 종목명 잘림(v17.1) · 보유 종목명 잘림 |
| 장식 띠 어긋남 | ::before/after 세로 띠 중심 vs 첫 글자 줄 중심, 2.5px 초과 | 코너 제목 띠가 7.5px 위로(v17.2) · 오늘의 장 카드 띠(v17.4) |
| 창 z-order | 모든 .overlay z-index > .tabbar | 설정·프로필 창 아래 66px이 탭바에 가림(v17.1) |
| 문서 폭 | documentElement.scrollWidth ≤ 화면 폭 | 그리드 blowout 전반 |
| 버튼 크기 | 높이 30px 미만 버튼(경고만) | — |

처음 한 번(Codespaces):

```bash
npm i -D playwright && npx playwright install chromium --with-deps
```

## 실기에서 바로 확인 — 화면 위 QA 오버레이

배포된 사이트에서도 같은 검사를 그 자리에서 돌릴 수 있다.

- 주소 뒤에 `?qa=1` 을 붙여 접속하거나, 콘솔에서 `liveQA(true)`
- 화면 밖으로 나가거나 글자가 잘린 요소에 **빨간 테두리**가 켜지고,
  오른쪽 아래 배지에 건수가 뜬다. 배지를 누르면 다시 검사, ✕ 로 끔.
- 평소에는 코드가 전혀 돌지 않는다(켰을 때만 동작).

## 재발 방지 규칙집 — 이 프로젝트에서 실제로 터졌던 패턴

**1. CSS 규칙을 '일부만' 덮어쓰지 않는다.**
용도를 바꿀 때 앞 규칙의 `transform` `height` `border-radius` `right` 를 명시적으로
해제한다. 남은 고아 속성이 코너 제목 띠(7.5px 위로)·오늘의 장 카드 띠(57%만 덮음)를
만들었다. → 덮어쓸 땐 `transform:none;height:auto` 처럼 **지울 것을 적는다**.

**2. 모바일 그리드는 `1fr` 이 아니라 `minmax(0,1fr)`.**
`1fr` = `minmax(auto,1fr)` 이고 auto 최소값은 자식의 최소 내용 너비다. 줄바꿈 없는
자식 하나가 열을 691px 까지 부풀려 해외 화면 전체를 잘랐다.

**3. flex 줄에는 셋 중 하나가 반드시 있어야 한다** — `flex-wrap:wrap`,
`overflow-x:auto`, 또는 항목 수 제한. 셋 다 없으면 분봉 버튼처럼 잘린다.

**4. 값의 천장은 `Math.min` 으로 자르지 않는다.**
자르면 그 위 값이 전부 한 점에 뭉친다(상승 확률 88% 사태). 점근(soft cap)으로 누른다.
시장 공통 배수는 확률에 곱하지 말고 로짓에 더한다.

**5. 진행 중인 Promise 를 버리지 않는다.**
"이미 요청 중이면 빈 값 반환"은 반드시 경쟁 상태를 만든다(체결 흐름 고착).
진행 중인 약속을 보관해 두고 그대로 돌려준다.

**6. 초기화 순서 — 읽는 줄은 채우는 줄 뒤에.**
`acctPassHash` 를 채우기 26줄 전에 쓰는 바람에 모든 계좌 비밀번호가 잠겼다.
전역 상태를 읽는 이전(migrate)·복원 코드는 그 상태가 채워진 뒤에 부른다.

**7. 무료 한도가 있는 저장소(KV)에는 캐시를 두지 않는다.**
수명(TTL) 붙은 데이터는 엣지 캐시·메모리로. 날짜 예산은 반드시 **UTC** 기준
(클라우드플레어 초기화 시각). 집계·통계를 주기적으로 쓰는 코드는 하루 총량을
먼저 계산해 본다(60초 간격 = 하루 1,440회 = 한도 초과).

## 배포 전 체크리스트

1. `node mobile-qa-check.mjs` — 통과
2. 직전 배포 zip 과 diff — 지워진 코드가 전부 의도한 것인지
3. `index.html` 의 `?v=` 캐시버스트 + `sw.js` 의 `SW_VER` 올리기
4. `version-info.js` 와 `_worker.js` 의 `APP_VER` 일치
5. 배포 후 실기에서 `?qa=1` 로 한 번 훑기
